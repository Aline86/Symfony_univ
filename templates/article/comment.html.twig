{% extends 'base.html.twig' %}

{% block h1 %}<h1>{% trans %}View the Article{% endtrans %}</h1>{% endblock %}
{% block langues %}
    <div class="langues">
        <ul class="flex2">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('add_comment', {'id': app.request.get('id'), '_locale': 'fr_FR'} ) }}">FR</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('add_comment', {'id': app.request.get('id'), '_locale': 'en_EN'} ) }}">EN</a>
            </li>
        </ul>
    </div>
{% endblock %}
{% block content %}
    {{ form_start(form) }}

        {{ form_row(form.title) }}

        {{ form_row(form.message) }}

        {{ form_row(form.author) }}

    {{ form_end(form) }}

{% endblock %}